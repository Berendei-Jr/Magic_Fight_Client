os: freebsd
language: minimal
compiler: gcc

before_install:
  - su -m root -c 'pkg install -y libsfml-dev'

addons:
  apt:
    packages:
      - g++
      - cmake

jobs:
  include:
    - name: "checks"
      script:
        - mkdir _build
        - cd _build
        - cmake .. && make && ./client test

notifications:
  email: false